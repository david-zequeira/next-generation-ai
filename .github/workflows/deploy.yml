name: Desplegar en GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Build estático
        run: npm run build
        env:
          # Con dominio propio (variable de repo NEXT_PUBLIC_SITE_URL) el sitio se
          # sirve en la raíz y el basePath sobra; sin ella, Pages lo sirve bajo /<repo>/.
          NEXT_PUBLIC_BASE_PATH: ${{ vars.NEXT_PUBLIC_SITE_URL && '' || format('/{0}', github.event.repository.name) }}
          # URL canónica del sitio: metadata de Open Graph, sitemap y aviso legal
          NEXT_PUBLIC_SITE_URL: ${{ vars.NEXT_PUBLIC_SITE_URL }}
          # URL pública del ng-agent (variable del repo; vacía = widget oculto)
          NEXT_PUBLIC_AGENT_URL: ${{ vars.NEXT_PUBLIC_AGENT_URL }}

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
